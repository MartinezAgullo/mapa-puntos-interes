services:
  postgis:
    image: postgis/postgis:16-3.4
    container_name: puntos_interes_postgis
    platform: linux/amd64
    environment:
      POSTGRES_DB: puntos_interes_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgis_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - app_network

volumes:
  postgis_data:
    driver: local

networks:
  app_network:
    driver: bridge
